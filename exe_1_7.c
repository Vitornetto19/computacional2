/* Nao consegui encontrar o erro do codigo

#include <stdio.h>

#include <math.h>


int main(){ 
 
 
  int enesimo = 0;
  float razao = 0;
  float primeiro = 0;
  
  printf("\nqual e o enesimo termo da progressao geometrica");
  scanf("%d", &enesimo);
  
  printf("\nqual e a razao da progressao");
  scanf("%f", &razao);
  
  printf("\nqual e o primeiro numero da progressao");
  scanf("%f", &primeiro);
  
  double result = pow(razao, (float)enesimo -1);
  
  float numero_final = primeiro * result;
  
  float soma_total = (primeiro * result) / (razao -1);
  
  printf("\no ultimo numero da progressao: %f", numero_final);
  
  printf("\na soma de todos os termos: %f", soma_total); 
  
  return 0; 

}      */


